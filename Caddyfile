http:// {
    # API routes
    handle /api/* {
        reverse_proxy api:8000
    }
    
    # Static files
    handle {
        root * /src/frontend
        try_files {path} /index.html
        file_server
    }
}
